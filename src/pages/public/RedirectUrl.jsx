import { MetaTags } from "../../components/shared";

function RedirectUrl() {
  return (
    <>
      <MetaTags title="Redirecting • Url shortner" />

      <section className="container w-full h-screen py-16 flex items-center justify-center">
        <p className="uppercase">Redirecting</p>
      </section>
    </>
  );
}

export default RedirectUrl;
